# Public defaults for Docker image builds.
# Override these in .env.local for personal development.

REACT_APP_USE_MOCKS=false

# Placeholder backend — set real values in .env.local or via Docker env vars.
REACT_APP_API_URL=https://api.infraweave.example.com

# OAuth providers are set per-environment (see .env.real / .env.local).
# Example schema: REACT_APP_OAUTH_PROVIDERS='[{"id":"aws","type":"cognito",...}]'
